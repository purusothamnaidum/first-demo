pipeline {
       agent any
    //   agent {
     //      label 'test'
      // }

    stages {
        stage('Print'){
            steps{
        sh '''ls -ltr
        touch test.txt'''
            }
        }
    }
}
